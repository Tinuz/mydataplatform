connector.name=hive

# Metastore configuratie (file-based)
hive.metastore=file
hive.metastore.catalog.dir=/data/trino/hive-metastore

# Schema management
hive.non-managed-table-writes-enabled=true

# Caching
hive.metastore-cache-ttl=2h
hive.metastore-refresh-interval=10s
